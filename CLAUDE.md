# Project: Mind Core Fitness

## Build

- After ANY source changes in `admin/`, always run `npm run build` from `admin/` directory
- Build outputs to `login/` folder (configured in `admin/vite.config.js`)
- Always commit the `login/` build output alongside source changes
